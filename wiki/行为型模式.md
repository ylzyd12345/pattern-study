# 行为型模式详解

行为型模式关注对象之间的通信和职责分配，描述对象之间如何协作以及如何划分责任，使得系统更加灵活和可扩展。

## 📋 模式概览

| 模式 | 英文名 | 难度 | 使用频率 | 核心思想 |
|------|--------|------|----------|----------|
| 责任链模式 | Chain of Responsibility | ⭐⭐ | ⭐⭐⭐⭐ | 请求传递链式处理 |
| 命令模式 | Command | ⭐⭐ | ⭐⭐⭐⭐ | 请求封装成对象 |
| 解释器模式 | Interpreter | ⭐⭐⭐⭐ | ⭐⭐ | 语言文法解释 |
| 迭代器模式 | Iterator | ⭐ | ⭐⭐⭐⭐⭐ | 集合遍历统一接口 |
| 中介者模式 | Mediator | ⭐⭐⭐ | ⭐⭐⭐ | 对象间解耦协调 |
| 备忘录模式 | Memento | ⭐⭐ | ⭐⭐⭐ | 对象状态保存恢复 |
| 观察者模式 | Observer | ⭐⭐ | ⭐⭐⭐⭐⭐ | 事件通知机制 |
| 状态模式 | State | ⭐⭐⭐ | ⭐⭐⭐⭐ | 状态行为变化 |
| 策略模式 | Strategy | ⭐⭐ | ⭐⭐⭐⭐⭐ | 算法族封装 |
| 模板方法模式 | Template Method | ⭐⭐ | ⭐⭐⭐⭐⭐ | 算法骨架定义 |
| 访问者模式 | Visitor | ⭐⭐⭐⭐ | ⭐⭐ | 结构操作分离 |

## ⛓️ 责任链模式 (Chain of Responsibility)

### 模式定义
使多个对象都有机会处理请求，从而避免请求的发送者和接收者之间的耦合关系。将这些对象连成一条链，并沿着这条链传递该请求，直到有一个对象处理它为止。

### UML类图
```
┌─────────────────┐
│    Handler      │
├─────────────────┤
│ - successor:    │
│   Handler       │
├─────────────────┤
│ + setSuccessor()│
│ + handleRequest()│
└─────────────────┘
         ▲
         │
    ┌────────┴────────┐
    │                 │
┌─────────────┐ ┌─────────────┐
│ConcreteHandler│ │ConcreteHandler│
│A            │ │B            │
├─────────────┤ ├─────────────┤
│ + handleRequest│ │ + handleRequest│
└─────────────┘ └─────────────┘
```

### 代码实现

#### 请求类
```java
public class Request {
    private String type;
    private String content;
    
    public Request(String type, String content) {
        this.type = type;
        this.content = content;
    }
    
    public String getType() { return type; }
    public String getContent() { return content; }
}
```

#### 抽象处理器
```java
public abstract class Handler {
    protected Handler successor;
    
    public void setSuccessor(Handler successor) {
        this.successor = successor;
    }
    
    public abstract void handleRequest(Request request);
}
```

#### 具体处理器
```java
public class ConcreteHandlerA extends Handler {
    @Override
    public void handleRequest(Request request) {
        if ("typeA".equals(request.getType())) {
            System.out.println("Handler A processing: " + request.getContent());
        } else if (successor != null) {
            successor.handleRequest(request);
        }
    }
}

public class ConcreteHandlerB extends Handler {
    @Override
    public void handleRequest(Request request) {
        if ("typeB".equals(request.getType())) {
            System.out.println("Handler B processing: " + request.getContent());
        } else if (successor != null) {
            successor.handleRequest(request);
        }
    }
}

public class ConcreteHandlerC extends Handler {
    @Override
    public void handleRequest(Request request) {
        if ("typeC".equals(request.getType())) {
            System.out.println("Handler C processing: " + request.getContent());
        } else if (successor != null) {
            successor.handleRequest(request);
        } else {
            System.out.println("No handler found for: " + request.getType());
        }
    }
}
```

### 应用场景
- 审批流程
- 异常处理
- 日志处理
- Web过滤器

### 优缺点
**优点**：
- 降低耦合度
- 增强灵活性
- 简化对象连接

**缺点**：
- 请求可能不被处理
- 性能影响
- 调试困难

## 📝 命令模式 (Command)

### 模式定义
将一个请求封装成一个对象，从而使你可用不同的请求对客户进行参数化，对请求排队或记录请求日志，以及支持可撤销的操作。

### UML类图
```
┌─────────────────┐    ┌─────────────────┐
│   Invoker       │    │   Command       │
├─────────────────┤    ├─────────────────┤
│ - command:      │    │ + execute()     │
│   Command       │    │ + undo()        │
├─────────────────┤    └─────────────────┘
│ + setCommand()  │              ▲
│ + invokeCommand()│              │
└─────────────────┘              │
         │                       │
         │              ┌────────┴────────┐
         │              │                 │
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
    │ConcreteCom  │ │ConcreteCom  │ │MacroCommand │
    │mandA        │ │mandB        │ │             │
    ├─────────────┤ ├─────────────┤ ├─────────────┤
    │ + execute() │ │ + execute() │ │ + execute() │
    │ + undo()    │ │ + undo()    │ │ + undo()    │
    └─────────────┘ └─────────────┘ └─────────────┘
         │                 │                 │
         │                 │                 │
    ┌─────────────┐ ┌─────────────┐         │
    │ ReceiverA   │ │ ReceiverB   │         │
    ├─────────────┤ ├─────────────┤         │
    │ + action()  │ │ + action()  │         │
    └─────────────┘ └─────────────┘         │
                                            │
                                ┌─────────────────┐
                                │   Command[]     │
                                │ commands        │
                                └─────────────────┘
```

### 代码实现

#### 命令接口
```java
public interface Command {
    void execute();
    void undo();
}
```

#### 接收者
```java
public class Light {
    public void turnOn() {
        System.out.println("Light is ON");
    }
    
    public void turnOff() {
        System.out.println("Light is OFF");
    }
}

public class Stereo {
    public void on() {
        System.out.println("Stereo is ON");
    }
    
    public void off() {
        System.out.println("Stereo is OFF");
    }
    
    public void setCD() {
        System.out.println("Stereo is set for CD");
    }
    
    public void setVolume(int volume) {
        System.out.println("Stereo volume set to " + volume);
    }
}
```

#### 具体命令
```java
public class LightOnCommand implements Command {
    private Light light;
    
    public LightOnCommand(Light light) {
        this.light = light;
    }
    
    @Override
    public void execute() {
        light.turnOn();
    }
    
    @Override
    public void undo() {
        light.turnOff();
    }
}

public class LightOffCommand implements Command {
    private Light light;
    
    public LightOffCommand(Light light) {
        this.light = light;
    }
    
    @Override
    public void execute() {
        light.turnOff();
    }
    
    @Override
    public void undo() {
        light.turnOn();
    }
}

public class StereoOnWithCDCommand implements Command {
    private Stereo stereo;
    
    public StereoOnWithCDCommand(Stereo stereo) {
        this.stereo = stereo;
    }
    
    @Override
    public void execute() {
        stereo.on();
        stereo.setCD();
        stereo.setVolume(11);
    }
    
    @Override
    public void undo() {
        stereo.off();
    }
}
```

#### 宏命令
```java
public class MacroCommand implements Command {
    private Command[] commands;
    
    public MacroCommand(Command[] commands) {
        this.commands = commands;
    }
    
    @Override
    public void execute() {
        for (Command command : commands) {
            command.execute();
        }
    }
    
    @Override
    public void undo() {
        for (int i = commands.length - 1; i >= 0; i--) {
            commands[i].undo();
        }
    }
}
```

#### 调用者
```java
public class RemoteControl {
    private Command[] onCommands;
    private Command[] offCommands;
    private Command undoCommand;
    
    public RemoteControl() {
        onCommands = new Command[7];
        offCommands = new Command[7];
        
        Command noCommand = new NoCommand();
        for (int i = 0; i < 7; i++) {
            onCommands[i] = noCommand;
            offCommands[i] = noCommand;
        }
        undoCommand = noCommand;
    }
    
    public void setCommand(int slot, Command onCommand, Command offCommand) {
        onCommands[slot] = onCommand;
        offCommands[slot] = offCommand;
    }
    
    public void onButtonWasPushed(int slot) {
        onCommands[slot].execute();
        undoCommand = onCommands[slot];
    }
    
    public void offButtonWasPushed(int slot) {
        offCommands[slot].execute();
        undoCommand = offCommands[slot];
    }
    
    public void undoButtonWasPushed() {
        undoCommand.undo();
    }
}

class NoCommand implements Command {
    @Override
    public void execute() {}
    
    @Override
    public void undo() {}
}
```

### 应用场景
- GUI按钮
- 宏操作
- 事务处理
- 队列请求

### 优缺点
**优点**：
- 解耦请求发送者和接收者
- 支持撤销和重做
- 支持宏命令

**缺点**：
- 可能产生大量具体命令类
- 增加系统复杂性

## 📖 解释器模式 (Interpreter)

### 模式定义
给定一种语言，定义它的文法的一种表示，并定义一个解释器，这个解释器使用该表示来解释语言中的句子。

### UML类图
```
┌─────────────────┐
│  Context        │
├─────────────────┤
│ + input: String │
│ + output: String│
└─────────────────┘
         ▲
         │
┌─────────────────┐
│ AbstractExpression│
├─────────────────┤
│ + interpret()   │
└─────────────────┘
         ▲
         │
    ┌────────┴────────┐
    │                 │
┌─────────────┐ ┌─────────────┐
│TerminalExpr │ │NonTerminal  │
│ession       │ │Expression   │
├─────────────┤ ├─────────────┤
│ + interpret()│ │ - expression│
└─────────────┘ │ + interpret()│
               └─────────────┘
                        ▲
                        │
               ┌────────┴────────┐
               │                 │
       ┌─────────────┐ ┌─────────────┐
       │AndExpression│ │OrExpression │
       ├─────────────┤ ├─────────────┤
       │ + interpret()│ │ + interpret()│
       └─────────────┘ └─────────────┘
```

### 代码实现

#### 表达式接口
```java
public interface Expression {
    boolean interpret(String context);
}
```

#### 终结符表达式
```java
public class TerminalExpression implements Expression {
    private String data;
    
    public TerminalExpression(String data) {
        this.data = data;
    }
    
    @Override
    public boolean interpret(String context) {
        return context.contains(data);
    }
}
```

#### 非终结符表达式
```java
public class OrExpression implements Expression {
    private Expression expr1;
    private Expression expr2;
    
    public OrExpression(Expression expr1, Expression expr2) {
        this.expr1 = expr1;
        this.expr2 = expr2;
    }
    
    @Override
    public boolean interpret(String context) {
        return expr1.interpret(context) || expr2.interpret(context);
    }
}

public class AndExpression implements Expression {
    private Expression expr1;
    private Expression expr2;
    
    public AndExpression(Expression expr1, Expression expr2) {
        this.expr1 = expr1;
        this.expr2 = expr2;
    }
    
    @Override
    public boolean interpret(String context) {
        return expr1.interpret(context) && expr2.interpret(context);
    }
}
```

### 应用场景
- 编译器设计
- 正则表达式
- SQL解析
- 配置文件解析

### 优缺点
**优点**：
- 易于改变和扩展文法
- 实现文法较为简单

**缺点**：
- 复杂文法难以维护
- 效率较低
- 应用场景有限

## 🔄 迭代器模式 (Iterator)

### 模式定义
提供一种方法顺序访问一个聚合对象中各个元素，而又不暴露该对象的内部表示。

### UML类图
```
┌─────────────────┐    ┌─────────────────┐
│   Client        │    │   Aggregate     │
├─────────────────┤    ├─────────────────┤
│                 │    │ + createIterator│
└─────────────────┤    │   (): Iterator  │
         │              └─────────────────┘
         │                       ▲
         │                       │
         │              ┌────────┴────────┐
         │              │                 │
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
    │  Iterator   │ │ConcreteAggr │ │ConcreteIter │
    ├─────────────┤ │egate        │ │ator         │
    │ + hasNext() │ ├─────────────┤ ├─────────────┤
    │ + next()    │ │ + createIterator│ │ + hasNext() │
    │ + remove()  │ │   (): Iterator│ │ + next()    │
    └─────────────┘ └─────────────┘ │ + remove()  │
                            │    └─────────────┘
                            │           │
                            │    ┌─────────────────┐
                            │    │ - position: int │
                            │    │ - aggregate:    │
                            │    │   ConcreteAggr  │
                            │    │   egate         │
                            │    └─────────────────┘
                            │
                     ┌─────────────────┐
                     │ - items: Object[]│
                     └─────────────────┘
```

### 代码实现

#### 迭代器接口
```java
public interface Iterator {
    boolean hasNext();
    Object next();
    void remove();
}
```

#### 聚合接口
```java
public interface Aggregate {
    Iterator createIterator();
}
```

#### 具体聚合
```java
import java.util.ArrayList;
import java.util.List;

public class ConcreteAggregate implements Aggregate {
    private List<Object> items = new ArrayList<>();
    
    public void addItem(Object item) {
        items.add(item);
    }
    
    public Object getItem(int index) {
        return items.get(index);
    }
    
    public int getSize() {
        return items.size();
    }
    
    @Override
    public Iterator createIterator() {
        return new ConcreteIterator(this);
    }
}
```

#### 具体迭代器
```java
public class ConcreteIterator implements Iterator {
    private ConcreteAggregate aggregate;
    private int current = 0;
    
    public ConcreteIterator(ConcreteAggregate aggregate) {
        this.aggregate = aggregate;
    }
    
    @Override
    public boolean hasNext() {
        return current < aggregate.getSize();
    }
    
    @Override
    public Object next() {
        if (hasNext()) {
            return aggregate.getItem(current++);
        }
        return null;
    }
    
    @Override
    public void remove() {
        if (current > 0) {
            aggregate.items.remove(--current);
        }
    }
}
```

### 应用场景
- 集合遍历
- 数据库结果集
- 文件系统遍历
- 树形结构遍历

### 优缺点
**优点**：
- 支持多种遍历方式
- 简化聚合接口
- 隐藏内部结构

**缺点**：
- 增加类的数量
- 遍历逻辑复杂

## 🤝 中介者模式 (Mediator)

### 模式定义
用一个中介对象来封装一系列的对象交互。中介者使各对象不需要显式地相互引用，从而使其耦合松散，而且可以独立地改变它们之间的交互。

### UML类图
```
┌─────────────────┐
│   Mediator      │
├─────────────────┤
│ + colleagueChanged()│
└─────────────────┘
         ▲
         │
┌─────────────────┐
│ConcreteMediator │
├─────────────────┤
│ - colleague1:   │
│   Colleague1    │
│ - colleague2:   │
│   Colleague2    │
├─────────────────┤
│ + colleagueChanged()│
└─────────────────┘
         ▲
         │
┌─────────────────┐
│   Colleague     │
├─────────────────┤
│ - mediator:     │
│   Mediator      │
├─────────────────┤
│ + Colleague()   │
│ + changed()     │
└─────────────────┘
         ▲
         │
    ┌────────┴────────┐
    │                 │
┌─────────────┐ ┌─────────────┐
│Colleague1   │ │Colleague2   │
├─────────────┤ ├─────────────┤
│ + action()  │ │ + action()  │
└─────────────┘ └─────────────┘
```

### 代码实现

#### 中介者接口
```java
public interface Mediator {
    void colleagueChanged(Colleague colleague);
}
```

#### 同事类
```java
public abstract class Colleague {
    protected Mediator mediator;
    
    public Colleague(Mediator mediator) {
        this.mediator = mediator;
    }
    
    public abstract void changed();
}
```

#### 具体同事
```java
public class Colleague1 extends Colleague {
    private String state;
    
    public Colleague1(Mediator mediator) {
        super(mediator);
    }
    
    public void action() {
        System.out.println("Colleague1 action");
        state = "active";
        changed();
    }
    
    public String getState() {
        return state;
    }
    
    @Override
    public void changed() {
        mediator.colleagueChanged(this);
    }
}

public class Colleague2 extends Colleague {
    private String state;
    
    public Colleague2(Mediator mediator) {
        super(mediator);
    }
    
    public void action() {
        System.out.println("Colleague2 action");
        state = "ready";
        changed();
    }
    
    public String getState() {
        return state;
    }
    
    @Override
    public void changed() {
        mediator.colleagueChanged(this);
    }
}
```

#### 具体中介者
```java
public class ConcreteMediator implements Mediator {
    private Colleague1 colleague1;
    private Colleague2 colleague2;
    
    public void setColleague1(Colleague1 colleague1) {
        this.colleague1 = colleague1;
    }
    
    public void setColleague2(Colleague2 colleague2) {
        this.colleague2 = colleague2;
    }
    
    @Override
    public void colleagueChanged(Colleague colleague) {
        if (colleague == colleague1) {
            System.out.println("Colleague1 changed, notifying Colleague2");
            colleague2.action();
        } else if (colleague == colleague2) {
            System.out.println("Colleague2 changed, notifying Colleague1");
            colleague1.action();
        }
    }
}
```

### 应用场景
- 聊天室
- GUI组件交互
- 交通控制系统
- 航空管制系统

### 优缺点
**优点**：
- 降低耦合度
- 集中控制交互
- 简化对象协议

**缺点**：
- 中介者可能过于复杂
- 难以维护

## 📝 备忘录模式 (Memento)

### 模式定义
在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，这样以后就可将该对象恢复到原先保存的状态。

### UML类图
```
┌─────────────────┐    ┌─────────────────┐
│   Originator    │    │   Memento       │
├─────────────────┤    ├─────────────────┤
│ - state: String │    │ - state: String │
├─────────────────┤    ├─────────────────┤
│ + setState()    │    │ + getState()    │
│ + getState()    │    │ + getName()     │
│ + createMemento()│   │ + getDate()      │
│ + restoreMemento()│  └─────────────────┘
└─────────────────┘              ▲
         │                       │
         │              ┌────────┴────────┐
         │              │                 │
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
    │ Caretaker   │ │ConcreteMeme│ │WideMemento │
    ├─────────────┤ │nto          │ ├─────────────┤
    │ - memento:  │ ├─────────────┤ │ - state:    │
    │   Memento   │ │ + getState()│ │   Object    │
    ├─────────────┤ │ + getName() │ ├─────────────┤
    │ + saveMemento│ │ + getDate() │ │ + getState()│
    │ + restoreMemento│ └─────────────┘ │ + setName() │
    └─────────────┘                │ + setDate() │
                                   └─────────────┘
```

### 代码实现

#### 备忘录接口
```java
public interface Memento {
    String getName();
    String getDate();
    String getState();
}
```

#### 具体备忘录
```java
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class ConcreteMemento implements Memento {
    private String state;
    private String name;
    private String date;
    
    public ConcreteMemento(String state) {
        this.state = state;
        this.name = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
        this.date = this.name;
    }
    
    @Override
    public String getName() {
        return name;
    }
    
    @Override
    public String getDate() {
        return date;
    }
    
    @Override
    public String getState() {
        return state;
    }
}
```

#### 发起人
```java
public class Originator {
    private String state;
    
    public Originator(String state) {
        this.state = state;
    }
    
    public void setState(String state) {
        this.state = state;
        System.out.println("Originator: State set to " + state);
    }
    
    public String getState() {
        return state;
    }
    
    public Memento createMemento() {
        System.out.println("Originator: Saving to Memento.");
        return new ConcreteMemento(state);
    }
    
    public void restoreMemento(Memento memento) {
        this.state = memento.getState();
        System.out.println("Originator: State after restoring from Memento: " + state);
    }
}
```

#### 看护人
```java
import java.util.ArrayList;
import java.util.List;

public class Caretaker {
    private List<Memento> mementos = new ArrayList<>();
    private Originator originator;
    
    public Caretaker(Originator originator) {
        this.originator = originator;
    }
    
    public void backup() {
        System.out.println("\nCaretaker: Saving Originator's state...");
        mementos.add(originator.createMemento());
    }
    
    public void undo() {
        if (!mementos.isEmpty()) {
            Memento memento = mementos.remove(mementos.size() - 1);
            System.out.println("Caretaker: Restoring state to: " + memento.getName());
            originator.restoreMemento(memento);
        } else {
            System.out.println("Caretaker: No mementos to restore.");
        }
    }
    
    public void showHistory() {
        System.out.println("Caretaker: Here's the list of mementos:");
        for (Memento memento : mementos) {
            System.out.println(memento.getName());
        }
    }
}
```

### 应用场景
- 撤销操作
- 游戏存档
- 事务回滚
- 版本控制

### 优缺点
**优点**：
- 保持封装边界
- 简化发起人类
- 状态管理更安全

**缺点**：
- 资源消耗大
- 实现复杂

## 👀 观察者模式 (Observer)

### 模式定义
定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都得到通知并被自动更新。

### UML类图
```
┌─────────────────┐    ┌─────────────────┐
│    Subject      │    │    Observer     │
├─────────────────┤    ├─────────────────┤
│ - observers:    │    │ + update()      │
│   List<Observer>│    └─────────────────┘
├─────────────────┤              ▲
│ + attach()      │              │
│ + detach()      │              │
│ + notify()      │              │
└─────────────────┘              │
         ▲                       │
         │              ┌────────┴────────┐
         │              │                 │
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ConcreteSubj │ │ConcreteObs  │ │ConcreteObs  │
│ect          │ │erverA       │ │erverB       │
├─────────────┤ ├─────────────┤ ├─────────────┤
│ - state: int │ │ + update()  │ │ + update()  │
│ + getState() │ └─────────────┘ └─────────────┘
│ + setState() │
│ + notify()   │
└─────────────┘
```

### 代码实现

#### 观察者接口
```java
public interface Observer {
    void update(int state);
}
```

#### 主题接口
```java
public interface Subject {
    void attach(Observer observer);
    void detach(Observer observer);
    void notifyObservers();
}
```

#### 具体主题
```java
import java.util.ArrayList;
import java.util.List;

public class ConcreteSubject implements Subject {
    private List<Observer> observers = new ArrayList<>();
    private int state;
    
    public int getState() {
        return state;
    }
    
    public void setState(int state) {
        this.state = state;
        System.out.println("Subject state changed to: " + state);
        notifyObservers();
    }
    
    @Override
    public void attach(Observer observer) {
        observers.add(observer);
    }
    
    @Override
    public void detach(Observer observer) {
        observers.remove(observer);
    }
    
    @Override
    public void notifyObservers() {
        for (Observer observer : observers) {
            observer.update(state);
        }
    }
}
```

#### 具体观察者
```java
public class ConcreteObserverA implements Observer {
    private Subject subject;
    
    public ConcreteObserverA(Subject subject) {
        this.subject = subject;
        subject.attach(this);
    }
    
    @Override
    public void update(int state) {
        System.out.println("ObserverA: Reacted to state change: " + state);
    }
}

public class ConcreteObserverB implements Observer {
    private Subject subject;
    
    public ConcreteObserverB(Subject subject) {
        this.subject = subject;
        subject.attach(this);
    }
    
    @Override
    public void update(int state) {
        System.out.println("ObserverB: Reacted to state change: " + state);
    }
}
```

### 应用场景
- 事件监听
- MVC模式
- 消息队列
- 股票价格监控

### 优缺点
**优点**：
- 松耦合
- 广播通信
- 符合开闭原则

**缺点**：
- 更新顺序问题
- 可能导致意外更新
- 内存泄漏风险

## 🎭 状态模式 (State)

### 模式定义
允许一个对象在其内部状态改变时改变它的行为，对象看起来似乎修改了它的类。

### UML类图
```
┌─────────────────┐
│    Context      │
├─────────────────┤
│ - state: State  │
├─────────────────┤
│ + setState()    │
│ + request()     │
└─────────────────┘
         ▲
         │
┌─────────────────┐
│     State       │
├─────────────────┤
│ + handle()      │
└─────────────────┘
         ▲
         │
    ┌────────┴────────┐
    │                 │
┌─────────────┐ ┌─────────────┐
│ConcreteState│ │ConcreteState│
│A            │ │B            │
├─────────────┤ ├─────────────┤
│ + handle()  │ │ + handle()  │
└─────────────┘ └─────────────┘
```

### 代码实现

#### 状态接口
```java
public interface State {
    void handle(Context context);
}
```

#### 具体状态
```java
public class ConcreteStateA implements State {
    @Override
    public void handle(Context context) {
        System.out.println("State A handling request");
        context.setState(new ConcreteStateB());
    }
}

public class ConcreteStateB implements State {
    @Override
    public void handle(Context context) {
        System.out.println("State B handling request");
        context.setState(new ConcreteStateA());
    }
}
```

#### 上下文
```java
public class Context {
    private State state;
    
    public Context(State state) {
        this.state = state;
    }
    
    public State getState() {
        return state;
    }
    
    public void setState(State state) {
        this.state = state;
        System.out.println("Context: Transitioned to " + state.getClass().getSimpleName());
    }
    
    public void request() {
        state.handle(this);
    }
}
```

### 应用场景
- 订单状态管理
- 游戏角色状态
- 工作流引擎
- 自动售货机

### 优缺点
**优点**：
- 封装转换规则
- 状态切换显式
- 状态对象可共享

**缺点**：
- 增加类和对象数量
- 结构复杂
- 对开闭原则支持不好

## 🎯 策略模式 (Strategy)

### 模式定义
定义一系列的算法,把它们一个个封装起来, 并且使它们可相互替换。

### UML类图
```
┌─────────────────┐    ┌─────────────────┐
│    Context      │    │    Strategy     │
├─────────────────┤    ├─────────────────┤
│ - strategy:     │    │ + algorithm()   │
│   Strategy      │    └─────────────────┘
├─────────────────┤              ▲
│ + setStrategy() │              │
│ + executeStrategy()│             │
└─────────────────┘              │
         │                       │
         │              ┌────────┴────────┐
         │              │                 │
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
    │ConcreteStra │ │ConcreteStra │ │ConcreteStra │
    │tegyA        │ │tegyB        │ │tegyC        │
    ├─────────────┤ ├─────────────┤ ├─────────────┤
    │ + algorithm()│ │ + algorithm()│ │ + algorithm()│
    └─────────────┘ └─────────────┘ └─────────────┘
```

### 代码实现

#### 策略接口
```java
public interface Strategy {
    int execute(int a, int b);
}
```

#### 具体策略
```java
public class ConcreteStrategyA implements Strategy {
    @Override
    public int execute(int a, int b) {
        return a + b;
    }
}

public class ConcreteStrategyB implements Strategy {
    @Override
    public int execute(int a, int b) {
        return a - b;
    }
}

public class ConcreteStrategyC implements Strategy {
    @Override
    public int execute(int a, int b) {
        return a * b;
    }
}
```

#### 上下文
```java
public class Context {
    private Strategy strategy;
    
    public Context(Strategy strategy) {
        this.strategy = strategy;
    }
    
    public void setStrategy(Strategy strategy) {
        this.strategy = strategy;
    }
    
    public int executeStrategy(int a, int b) {
        return strategy.execute(a, b);
    }
}
```

### 应用场景
- 算法切换
- 支付方式选择
- 数据压缩算法
- 路径规划

### 优缺点
**优点**：
- 算法可自由切换
- 避免多重条件判断
- 扩展性好

**缺点**：
- 策略类增多
- 客户端必须知道策略
- 增加对象数量

## 📋 模板方法模式 (Template Method)

### 模式定义
定义一个操作中的算法的骨架，而将一些步骤延迟到子类中。模板方法使得子类可以不改变一个算法的结构即可重定义该算法的某些特定步骤。

### UML类图
```
┌─────────────────┐
│AbstractClass    │
├─────────────────┤
│ + templateMethod()│
│ + primitiveOp1() │
│ + primitiveOp2() │
│ + hook()        │
└─────────────────┘
         ▲
         │
┌─────────────────┐
│ConcreteClass    │
├─────────────────┤
│ + primitiveOp1() │
│ + primitiveOp2() │
│ + hook()        │
└─────────────────┘
```

### 代码实现

#### 抽象类
```java
public abstract class AbstractClass {
    public final void templateMethod() {
        primitiveOperation1();
        primitiveOperation2();
        if (hook()) {
            concreteOperation();
        }
    }
    
    protected abstract void primitiveOperation1();
    protected abstract void primitiveOperation2();
    
    protected void concreteOperation() {
        System.out.println("Concrete operation in AbstractClass");
    }
    
    protected boolean hook() {
        return true;
    }
}
```

#### 具体类
```java
public class ConcreteClass extends AbstractClass {
    @Override
    protected void primitiveOperation1() {
        System.out.println("ConcreteClass: primitiveOperation1");
    }
    
    @Override
    protected void primitiveOperation2() {
        System.out.println("ConcreteClass: primitiveOperation2");
    }
    
    @Override
    protected boolean hook() {
        return false;
    }
}
```

### 应用场景
- 算法框架
- 框架设计
- 生命周期管理
- 数据处理流程

### 优缺点
**优点**：
- 提高复用性
- 扩展性好
- 控制子类扩展

**缺点**：
- 增加类数量
- 继承关系

## 🚶 访问者模式 (Visitor)

### 模式定义
表示一个作用于某对象结构中的各元素的操作。它使你可以在不改变各元素的类的前提下定义作用于这些元素的新操作。

### UML类图
```
┌─────────────────┐    ┌─────────────────┐
│   Visitor       │    │   Element       │
├─────────────────┤    ├─────────────────┤
│ + visitA()      │    │ + accept()      │
│ + visitB()      │    └─────────────────┘
└─────────────────┘              ▲
         ▲                       │
         │              ┌────────┴────────┐
         │              │                 │
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ConcreteVisi │ │ConcreteVisi │ │ConcreteEle  │
│torA         │ │torB         │ │mentA        │
├─────────────┤ ├─────────────┤ ├─────────────┤
│ + visitA()  │ │ + visitA()  │ │ + accept()  │
│ + visitB()  │ │ + visitB()  │ │ + operationA()│
└─────────────┘ └─────────────┘ └─────────────┘
                             │
                    ┌─────────────────┐
                    │ConcreteEle     │
                    │mentB            │
                    ├─────────────────┤
                    │ + accept()      │
                    │ + operationB()  │
                    └─────────────────┘
```

### 代码实现

#### 访问者接口
```java
public interface Visitor {
    void visitConcreteElementA(ConcreteElementA elementA);
    void visitConcreteElementB(ConcreteElementB elementB);
}
```

#### 元素接口
```java
public interface Element {
    void accept(Visitor visitor);
}
```

#### 具体元素
```java
public class ConcreteElementA implements Element {
    @Override
    public void accept(Visitor visitor) {
        visitor.visitConcreteElementA(this);
    }
    
    public void operationA() {
        System.out.println("ConcreteElementA operation");
    }
}

public class ConcreteElementB implements Element {
    @Override
    public void accept(Visitor visitor) {
        visitor.visitConcreteElementB(this);
    }
    
    public void operationB() {
        System.out.println("ConcreteElementB operation");
    }
}
```

#### 具体访问者
```java
public class ConcreteVisitorA implements Visitor {
    @Override
    public void visitConcreteElementA(ConcreteElementA elementA) {
        System.out.println("VisitorA visiting ElementA");
        elementA.operationA();
    }
    
    @Override
    public void visitConcreteElementB(ConcreteElementB elementB) {
        System.out.println("VisitorA visiting ElementB");
        elementB.operationB();
    }
}

public class ConcreteVisitorB implements Visitor {
    @Override
    public void visitConcreteElementA(ConcreteElementA elementA) {
        System.out.println("VisitorB visiting ElementA");
        elementA.operationA();
    }
    
    @Override
    public void visitConcreteElementB(ConcreteElementB elementB) {
        System.out.println("VisitorB visiting ElementB");
        elementB.operationB();
    }
}
```

#### 对象结构
```java
import java.util.ArrayList;
import java.util.List;

public class ObjectStructure {
    private List<Element> elements = new ArrayList<>();
    
    public void attach(Element element) {
        elements.add(element);
    }
    
    public void detach(Element element) {
        elements.remove(element);
    }
    
    public void accept(Visitor visitor) {
        for (Element element : elements) {
            element.accept(visitor);
        }
    }
}
```

### 应用场景
- 编译器
- 文档处理
- 数据结构遍历
- 复杂对象操作

### 优缺点
**优点**：
- 符合单一职责
- 优秀的扩展性
- 灵活性高

**缺点**：
- 增加新元素困难
- 依赖倒置
- 违反依赖倒置原则

## 🔗 模式对比

| 模式 | 主要目的 | 结构特点 | 复杂度 | 典型应用 |
|------|----------|----------|--------|----------|
| 责任链 | 请求处理 | 链式传递 | 中 | 审批流程 |
| 命令 | 请求封装 | 对象封装 | 中 | 撤销操作 |
| 解释器 | 语言解释 | 语法树 | 高 | DSL实现 |
| 迭代器 | 集合遍历 | 统一接口 | 低 | 集合框架 |
| 中介者 | 对象协调 | 中央控制 | 中 | 聊天室 |
| 备忘录 | 状态保存 | 快照机制 | 中 | 撤销操作 |
| 观察者 | 事件通知 | 订阅发布 | 中 | 事件系统 |
| 状态 | 状态机 | 状态切换 | 中 | 工作流 |
| 策略 | 算法选择 | 算法封装 | 中 | 算法切换 |
| 模板方法 | 算法骨架 | 框架定义 | 中 | 框架设计 |
| 访问者 | 结构操作 | 操作分离 | 高 | 编译器 |

## 🎯 选择指南

### 选择责任链模式当：
- 多个对象可以处理请求
- 不确定哪个对象处理
- 可动态添加处理器

### 选择命令模式当：
- 需要撤销/重做
- 需要事务支持
- 需要队列请求

### 选择解释器模式当：
- 需要解析语言
- 文法相对简单
- 性能要求不高

### 选择迭代器模式当：
- 需要遍历集合
- 不暴露内部结构
- 支持多种遍历

### 选择中介者模式当：
- 对象间复杂交互
- 需要解耦对象
- 集中控制交互

### 选择备忘录模式当：
- 需要保存状态
- 支持撤销操作
- 状态封装重要

### 选择观察者模式当：
- 一对多依赖
- 状态变化通知
- 松耦合通信

### 选择状态模式当：
- 对象状态依赖行为
- 状态转换复杂
- 需要状态机

### 选择策略模式当：
- 算法可互换
- 避免条件分支
- 运行时选择

### 选择模板方法模式当：
- 算法结构固定
- 子步骤可变
- 控制子类扩展

### 选择访问者模式当：
- 结构稳定
- 操作多变
- 需要相关操作

## 📚 实际应用

### JDK中的应用
- `javax.servlet.Filter` - 责任链模式
- `java.lang.Runnable` - 命令模式
- `java.text.Format` - 策略模式
- `java.util.Iterator` - 迭代器模式
- `java.util.Observer` - 观察者模式
- `java.util.Collections` - 模板方法模式

### Spring中的应用
- `HandlerInterceptor` - 责任链模式
- `ApplicationEvent` - 观察者模式
- `JdbcTemplate` - 模板方法模式
- `TransactionTemplate` - 命令模式

### 最佳实践
1. **理解本质**：掌握模式的核心思想
2. **适度使用**：不要过度设计
3. **结合实际**：根据业务需求选择
4. **持续学习**：关注模式的发展

---

行为型模式帮助我们更好地组织对象间的交互和职责分配。记住：模式是工具，要用在合适的地方。